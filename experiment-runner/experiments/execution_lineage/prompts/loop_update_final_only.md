Update the prior final output after an upstream source change.

Requirements:
- update only claims affected by the source change
- preserve unaffected claims where possible
- use only the provided current source bundle
- do not rely on superseded or irrelevant material
- cite source IDs in the updated memo
